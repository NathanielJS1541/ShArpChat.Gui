<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShArpChat.Gui.MainPage"
             x:Name="PageRoot">

    <StackLayout Margin="20">
        <HorizontalStackLayout>
            <Button Text="Refresh"
                    HorizontalOptions="End"
                    Command="{Binding RefreshCommand, Source={x:Reference PageRoot}}"/>
        </HorizontalStackLayout>

        <RefreshView Command="{Binding RefreshCommand, Source={x:Reference PageRoot}}">
            <CollectionView ItemsSource="{Binding NetworkInterfaces, Source={x:Reference PageRoot}}">

                <CollectionView.Header>
                    <Label Text="Network Interfaces"
                           Style="{StaticResource Headline}"/>
                </CollectionView.Header>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="*,*,*">
                            <Label Grid.Column="0" Text="{Binding Name}" />
                            <Label Grid.Column="1" Text="{Binding PhysicalAddressAsString}" />
                            <Label Grid.Column="2" Text="{Binding Address}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

            </CollectionView>
        </RefreshView>

    </StackLayout>

</ContentPage>
